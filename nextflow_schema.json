{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com//Users/ftricomi/Documents/projects/ensembl-genes-metadata/master/nextflow_schema.json",
    "title": "/Users/ftricomi/Documents/projects/ensembl-genes-metadata pipeline parameters",
    "description": "",
    "type": "object",
    "definitions": {
        "generic_options": {
            "title": "Generic options",
            "type": "object",
            "fa_icon": "fas fa-file-import",
            "description": "Less common options for the pipeline, typically set in a config file.",
            "help_text": "These options are common to all nf-core pipelines and allow you to customise some of the core preferences for how the pipeline runs.\n\nTypically these options would be set in a Nextflow config file loaded for all pipeline runs, such as `~/.nextflow/config`.",
            "properties": {
                "help": {
                    "type": "boolean",
                    "description": "Display help text.",
                    "fa_icon": "fas fa-question-circle",
                    "hidden": true
                }
            }
        }
    },
    "allOf": [
        {
            "$ref": "#/definitions/generic_options"
        }
    ],
    "properties": {
        "bam2cram": {
            "type": "boolean",
            "default": true
        },
        "mergeTissue": {
            "type": "boolean",
            "default": true
        },
        "stranded": {
            "type": "boolean",
            "default": true
        },
        "bam2bigWig": {
            "type": "boolean",
            "default": true
        },
        "cleanOutputDir": {
            "type": "boolean",
            "default": true
        },
        "csvFile": {
            "type": "string"
        },
        "ftpBaseUrl": {
            "type": "string",
            "default": "ftp://ftp.sra.ebi.ac.uk/vol1/fastq"
        },
        "ncbiBaseUrl": {
            "type": "string",
            "default": "https://api.ncbi.nlm.nih.gov/datasets/v2alpha/genome/accession/"
        },
        "enscode": {
            "type": "string"
        },
        "cleanCache": {
            "type": "boolean"
        },
        "outDir": {
            "type": "string"
        },
        "cacheDir": {
            "type": "string",
            "default": "/cache"
        },
        "files_latency": {
            "type": "integer",
            "default": 60
        }
    }
}