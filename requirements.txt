# Runtime dependencies -- needed to actually run the code
#
# NOTE: pandas is intentionally pinned to 2.3.3, not the newer 3.x line.
# Pandas 3.0+ requires Python 3.11+, but this project must run on Codon's
# Python 3.9, so pandas 3.x cannot be installed there. 2.3.3 is the last
# pandas release confirmed to support Python 3.9.
pandas==2.3.3
PyMySQL==1.2.0
matplotlib==3.11.0

# Development / quality gate tools -- needed to work on the code
# (these are dev-only tools, version mismatches here matter less than for
# pandas, but if you hit install errors on Codon, try without exact pins)
pytest==9.1.1
pylint==4.0.6
mypy==2.1.0
mypy_extensions==1.1.0
black==26.5.1
